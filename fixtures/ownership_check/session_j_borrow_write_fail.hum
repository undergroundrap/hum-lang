module fixtures.ownership_check.session_j_borrow_write_fail

task write_borrow(value: Int) -> Int {
  why:
    prove default borrow parameters cannot be written through set

  does:
    set value = value + 1
    return value
}