module fixtures.callable.session_al_compound_transport_fail

task apply_once(transform: task(UInt) -> UInt, value: UInt) -> UInt {
  does:
    change saved: UInt = 0
    set saved = transform + 0
    return transform(value)
}
