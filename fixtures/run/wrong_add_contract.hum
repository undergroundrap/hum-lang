module fixtures.run.wrong_add_contract

task add(a: Int, b: Int) -> Int {
  why:
    prove ensures catches a sabotaged implementation

  ensures:
    result == a + b

  cost:
    time: O(1)
    space: O(1)
    check: warn

  does:
    return a - b
}
