module fixtures.run.session_af_predicate_v2_malformed_neighbor_fail

task malformed_neighbor() -> UInt {
  why:
    pin the malformed adjacent recognition case
  ensures:
    result = 2
  does:
    return 2
}
