app path_contract_malformed_probe {
  why:
    keep Predicate v2 trailing-prose ownership separate from opaque Path inspection

  starts with:
    run_tool

  task run_tool(input: Path) -> Unit {
    ensures:
      input == input according to unchecked prose

    allocates:
      nothing

    does:
      return
  }
}
