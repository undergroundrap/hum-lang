app wrong_stdout_argument {
  uses:
    stdout.write

  starts with:
    run_tool

  task run_tool -> Result Unit, OutputError {
    uses:
      stdout.write

    fails when:
      the output operation fails

    allocates:
      callee-defined allocation behavior

    does:
      let written = try stdout_write(7)
      return written
  }
}
