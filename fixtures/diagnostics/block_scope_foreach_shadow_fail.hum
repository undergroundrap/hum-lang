task foreach_body_shadow(items: List UInt) -> Text {
  why:
    WO28 #16: for-each body let must not leak its type fact

  does:
    let x = 3
    for each n in items {
      let x = "s"
    }
    return x
}
