module programs.illegal_pre_app_test_fail

test proof unit {
  covers:
    canonical order rejection
  does:
}

app illegal_pre_app_test_fail {
  starts with:
    run_tool
  task run_tool -> Unit {
    allocates:
      callee-defined allocation behavior
    does:
      return
  }
}
