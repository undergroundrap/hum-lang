module programs.helper_before_start_fail

app helper_before_start_fail {
  starts with:
    run_tool
  task helper -> Unit {
    allocates:
      callee-defined allocation behavior
    does:
      return
  }
  task run_tool -> Unit {
    allocates:
      callee-defined allocation behavior
    does:
      return helper()
  }
}
