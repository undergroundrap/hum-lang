type LocalType {
  value: Int
}

module programs.late_module_fail

app late_module_fail {
  starts with:
    run_tool
  task run_tool -> Unit {
    allocates:
      callee-defined allocation behavior
    does:
      return
  }
}
